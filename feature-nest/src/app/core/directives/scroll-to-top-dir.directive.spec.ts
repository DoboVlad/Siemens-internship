import { ScrollToTopDirDirective } from './scroll-to-top-dir.directive';

describe('ScrollToTopDirDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToTopDirDirective();
    expect(directive).toBeTruthy();
  });
});
